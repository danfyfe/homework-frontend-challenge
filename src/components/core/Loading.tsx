import { Modal, P } from "@/components/core";

const Loading = () => (<Modal className="flex justify-center items-center absolute top-1/2 -translate-y-1/2"><P className="">Loading...</P></Modal>);

export default Loading;
